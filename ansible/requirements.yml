# Ansible requirements — collections needed for Kubernetes/Helm automation
---
collections:
  - name: kubernetes.core
    version: ">=3.0.0"
  - name: community.general
    version: ">=8.0.0"
  - name: amazon.aws
    version: ">=8.0.0"
