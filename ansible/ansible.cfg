[defaults]
# Ansible configuration for NET4255 Kubernetes automation
inventory = inventories/dev/hosts.yml
roles_path = roles
retry_files_enabled = False
stdout_callback = yaml
nocows = 1
host_key_checking = False

[privilege_escalation]
become = False

[connection]
# We run Ansible locally against the K8s API — no SSH needed
transport = local
